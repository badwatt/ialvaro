export type ExperienceEntry = {
  id: string;
  title: string;
  image: string;
  date_from: string;
  date_to: string;
  url: string;
  description: string;
};

export type PortfolioEntry = {
  id: string;
  title: string;
  image: string;
  url: string;
  description: string;
};

export type AboutEntry = {
  id: string;
  bio: string;
};
